# 路径引用与包（Package）

欢迎来到第三个实验！

在这个实验中，你将学习：

1. **无效的路径引用** —— 当访问的路径不存在时 Rego 如何处理
2. **判定属性是否存在** —— 使用 `not` 与 `==` 的技巧
3. **Package 与跨包引用** —— 用 `package` 组织代码，用 `import data.` 跨包调用规则

> 💡 本实验环境已预装 conftest，可以直接在终端中运行策略检查命令。
