# 内建函数与用户定义函数

本次实验你将学习 Rego 中的函数体系：

1. **常用内建函数**：比较、数学、聚合、字符串、类型转换、glob、sprintf
2. **用户定义函数**：自定义函数语法、函数调用
3. **函数重载**：用同名函数实现逻辑或

## 准备工作

环境已预装 OPA。进入工作目录：

```bash
cd /root/workspace
mkdir -p policy
```
