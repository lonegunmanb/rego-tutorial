# Rego 初识：规则与条件赋值

欢迎来到 Rego 教程的第一个实验！

在这个实验中，你将学习：

1. **Rego 的基本结构** —— `package`、`import rego.v1` 声明
2. **编写第一条规则** —— 理解规则就是全局变量
3. **条件赋值** —— 使用 `if {}` 根据输入数据决定输出
4. **`default` 关键字** —— 为规则提供默认值
5. **使用 conftest** —— 用命令行工具执行策略检查

> 💡 本实验环境已预装 conftest，可以直接在终端中运行策略检查命令。
