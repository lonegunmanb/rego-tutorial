# 规则条件详解与小测验

欢迎来到第五个实验！

在这个实验中，你将学习：

1. **常规条件** —— Rego 中没有 falsy，`0`、`""`、`null` 都是成功的
2. **逻辑非条件** —— `not` 的反转行为与反直觉陷阱
3. **赋值语句条件** —— 赋值 `false` 也是成功的！
4. **集合成员条件** —— `in` 关键字和 `some` 的用法
5. **小测验** —— 用 OPA 验证你对规则条件的理解

> 💡 本实验环境已预装 conftest 和 opa，可以直接在终端中运行。
