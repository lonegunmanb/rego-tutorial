# 常见陷阱（上）：类型与比较

本次实验你将学习 Rego 中与类型和比较相关的常见陷阱：

1. **集合与对象的比较陷阱**：为什么不能对 set 和 object 比较大小
2. **跨类型比较陷阱**：`"0" > 3` 为什么是 `true`，以及如何定义类型安全比较函数
3. **部分定义默认值陷阱**：空集合 `{}` 与未定义值的区别
4. **命名遮蔽陷阱**：局部变量和自定义函数如何意外遮蔽内建名称

## 准备工作

环境已预装 OPA。进入工作目录：

```bash
cd /root/workspace
mkdir -p policy
```
