更新后的大纲，共 **12 篇**：

| # | 章节标题 | 涵盖内容 | 预计实验时长 |
|---|---------|---------|------------|
| 1 | **Rego 初识：规则与条件赋值** | Rego 简介、v0/v1 版本差异、教学工具(Playground/conftest)、第一条规则、条件赋值 `if {}`、`default` 关键字 | ~10 min |
| 2 | **逻辑运算：与、或、非** | 逻辑与（多条件并列）、逻辑或（同名规则）、不可赋予不同值的约束（conflict error）、逻辑非（`not`） | ~10 min |
| 3 | **路径引用与包（Package）** | 无效路径引用、路径引用等同 `false`、`not input.x == input.x` 技巧、Package 声明、`import data.` 跨包引用 | ~10 min |
| 4 | **赋值与局部变量** | 各种类型赋值（number/string/array/object）、隐式 `true`、局部变量作用域 | ~10 min |
| 5 | **规则条件详解与小测验** | 四类规则条件（常规条件/逻辑非条件/赋值语句条件/集合成员条件）、规则条件小测验 | ~15 min |
| 6 | **迭代（Iteration）** | `some`/`in` 迭代集合、数组、对象；键值对迭代；嵌套迭代；自由迭代与 `_` 匿名变量 | ~15 min |
| 7 | **集合声明与生成表达式** | JSON 语法声明集合、Set/Array/Object 生成表达式（comprehension）、条件过滤生成 | ~15 min |
| 8 | **部分定义（Partial Definition）** | `contains` 部分定义 Set、`[]` 部分定义 Object、v0 `deny[msg]` 语法对比 v1、带条件的批量部分定义、不支持部分定义数组 | ~15 min |
| 9 | **内建函数与用户定义函数** | 常用内建函数（比较/数学/聚合/字符串/类型转换/glob/sprintf）、自定义函数语法、函数重载实现逻辑或 | ~15 min |
| 10 | **`every` 全量迭代** | `every` AllOf 语义、`every` 中实现逻辑或（`in` set / helper 函数）、`some` + `every` 组合模式 | ~15 min |
| 11 | **常见陷阱（上）：类型与比较** | 集合/对象比较陷阱、跨类型比较陷阱（`"0" > 3`）、类型安全比较函数、部分定义默认值陷阱（空集合 `{}` vs 未定义）、命名遮蔽陷阱 | ~15 min |
| 12 | **常见陷阱（下）：引用与迭代** | 未声明迭代变量陷阱、无效路径引用陷阱（`exists` 函数、`not x == x`）、错别字陷阱、输入数据预校验最佳实践、反转含迭代规则的陷阱 | ~15 min |

---

### 调整要点

- **原第 4 篇** → 拆为 **4（赋值与局部变量）** + **5（规则条件详解与小测验）**，各 10–15 分钟
- **原第 10 篇** → 拆为 **11（类型与比较陷阱）** + **12（引用与迭代陷阱）**，各 ~15 分钟
- 12 篇节奏均匀，每篇 10–15 分钟

是否按此大纲开始创建教程文件？